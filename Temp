_applyVerticalSearchTabLayout: function(tab) {
			if (!tab || !tab.mainContainer || !tab.searchCriteriaPane || !tab.searchResultsPane) {
				return;
			}
			if (tab.searchContainerNode) {
				domClass.add(tab.searchContainerNode, "metlifeVerticalSearchLayout");
			}
			if (tab.searchResultsContainer && tab.searchResultsContainer.domNode) {
				domClass.add(tab.searchResultsContainer.domNode, "metlifeBasicSearchResults");
			}
			if (tab.searchResults && tab.searchResults.domNode) {
				domClass.add(tab.searchResults.domNode, "metlifeBasicSearchResults");
			}
			if (tab.mainContainer.domNode) {
				domClass.add(tab.mainContainer.domNode, "metlifeVerticalSearchMain");
			}
			if (tab.mainContainer.set) {
				tab.mainContainer.set("design", "sidebar");
				tab.mainContainer.set("gutters", false);
			}
			this._relocateSearchCriteriaPaneToLeading(tab);
			tab.searchResultsPane.set("region", "center");
			tab.searchResultsPane.set("splitter", false);
			this._hideSearchCriteriaSplitter(tab);
			this._applyVerticalSearchCriteriaOverflowFix(tab);
			this._expandSearchResultsPane(tab);
			this._installCriteriaCollapseSupport(tab);
			this._installVerticalSearchLayoutResize(tab);
			var self = this;
			setTimeout(function() {
				self._hideSearchCriteriaSplitter(tab);
				self._syncCriteriaCollapsedLayout(tab);
				if (tab.mainContainer && tab.mainContainer.resize) {
					tab.mainContainer.resize();
				}
				if (tab.resize) {
					tab.resize();
				}
			}, 0);
		},

		_relocateSearchCriteriaPaneToLeading: function(tab) {
			var mc = tab.mainContainer;
			var criteria = tab.searchCriteriaPane;
			if (!mc || !criteria) {
				return;
			}
			criteria.set("splitter", false);
			var region = criteria.get ? criteria.get("region") : criteria.region;
			if (region !== "leading" && region !== "left") {
				try {
					mc.removeChild(criteria);
				} catch (e) {
					// ignore if not a child yet
				}
				criteria.set("region", "leading");
				criteria.set("splitter", false);
				mc.addChild(criteria, 0);
			}
		},

		_hideSearchCriteriaSplitter: function(tab) {
			if (!tab || !tab.searchCriteriaPane) {
				return;
			}
			tab.searchCriteriaPane.set("splitter", false);
			if (tab.searchCriteriaPane.domNode) {
				tab.searchCriteriaPane.domNode.setAttribute("splitter", "false");
				tab.searchCriteriaPane.domNode.setAttribute("region", "leading");
			}
			var splitterWidget = tab.searchCriteriaPane._splitterWidget;
			if (splitterWidget) {
				if (splitterWidget.domNode) {
					domStyle.set(splitterWidget.domNode, "display", "none");
				}
				if (splitterWidget.hide) {
					splitterWidget.hide();
				}
			}
			var splitterNode = tab.searchCriteriaPane.id ? document.getElementById(tab.searchCriteriaPane.id + "_splitter") : null;
			if (splitterNode) {
				domStyle.set(splitterNode, "display", "none");
			}
		},

		_expandSearchResultsPane: function(tab) {
			if (tab._searchResultsNode) {
				domClass.remove(tab._searchResultsNode, "dijitHidden");
			}
			if (tab.searchResultsContainer && tab.searchResultsContainer.domNode) {
				domClass.remove(tab.searchResultsContainer.domNode, "dijitHidden");
			}
			if (tab.searchResultsHeader) {
				domClass.remove(tab.searchResultsHeader, "dijitClosed");
				domClass.add(tab.searchResultsHeader, "dijitOpen");
			}
			if (tab.searchResultsHeaderTitleNode) {
				domAttr.set(tab.searchResultsHeaderTitleNode, "aria-pressed", "true");
			}
			// Do not force Search Criteria open — users may collapse it to maximize results.
		},

		/**
		 * Lets the OOTB Search Criteria title toggle shrink the leading pane width
		 * so Search Results can expand. (OOTB toggle only adjusts height for top/bottom layout.)
		 */
		_installCriteriaCollapseSupport: function(tab) {
			if (!tab || tab._metlifeCriteriaCollapseHook) {
				return;
			}
			tab._metlifeCriteriaCollapseHook = true;
			var self = this;
			if (tab._toggleSearchCriteria) {
				this.own(aspect.after(tab, "_toggleSearchCriteria", function() {
					self._syncCriteriaCollapsedLayout(tab);
				}));
			}
			this._syncCriteriaCollapsedLayout(tab);
		},

		_isCriteriaCollapsed: function(tab) {
			if (!tab) {
				return false;
			}
			if (tab._searchCriteriaNode && domClass.contains(tab._searchCriteriaNode, "dijitHidden")) {
				return true;
			}
			if (tab.searchCriteriaHeader && domClass.contains(tab.searchCriteriaHeader, "dijitClosed")) {
				return true;
			}
			return false;
		},

		_getCriteriaPaneWidth: function(tab) {
			return this._isCriteriaCollapsed(tab) ? 48 : 340;
		},

		_syncCriteriaCollapsedLayout: function(tab) {
			if (!tab || !tab.searchContainerNode) {
				return;
			}
			var collapsed = this._isCriteriaCollapsed(tab);
			domClass.toggle(tab.searchContainerNode, "metlifeCriteriaCollapsed", collapsed);
			if (tab.searchCriteriaPane && tab.searchCriteriaPane.domNode) {
				domClass.toggle(tab.searchCriteriaPane.domNode, "metlifeCriteriaCollapsed", collapsed);
			}
			this._fixVerticalSearchTabLayoutSizes(tab);
			if (tab.mainContainer && tab.mainContainer.resize) {
				try {
					tab.mainContainer.resize();
				} catch (e) {
					console.warn("[MetlifeSearch] mainContainer resize after criteria toggle skipped", e);
				}
			}
		},

		_installVerticalSearchLayoutResize: function(tab) {
			if (tab._metlifeVerticalLayoutResizeHook) {
				return;
			}
			tab._metlifeVerticalLayoutResizeHook = true;
			var self = this;
			var fixSizes = lang.hitch(this, function() {
				this._fixVerticalSearchTabLayoutSizes(tab);
			});
			this.own(aspect.after(tab, "resize", fixSizes));
			if (tab.mainContainer) {
				this.own(aspect.after(tab.mainContainer, "resize", fixSizes));
			}
			if (tab.searchCriteriaPane) {
				this.own(aspect.after(tab.searchCriteriaPane, "resize", fixSizes));
			}
			if (tab.searchResultsPane) {
				this.own(aspect.after(tab.searchResultsPane, "resize", fixSizes));
			}
			if (tab.searchDefinition && tab.searchDefinition.onSearchStarted) {
				this.own(aspect.after(tab.searchDefinition, "onSearchStarted", lang.hitch(this, function() {
					this._expandSearchResultsPane(tab);
					this._syncCriteriaCollapsedLayout(tab);
				})));
			}
			if (tab.resizeSearchResults) {
				this.own(aspect.around(tab, "resizeSearchResults", function(original) {
					return function() {
						if (tab.searchContainerNode && domClass.contains(tab.searchContainerNode, "metlifeVerticalSearchLayout")) {
							self._fixVerticalSearchTabLayoutSizes(tab);
							return;
						}
						return original.apply(tab, arguments);
					};
				}));
			}
			this.own(aspect.around(tab, "resize", function(original) {
				return function() {
					if (tab.searchContainerNode && domClass.contains(tab.searchContainerNode, "metlifeVerticalSearchLayout")) {
						var height = domGeometry.getMarginBox(tab.searchContainerNode).h;
						var width = domGeometry.getMarginBox(tab.searchContainerNode).w;
						if (height > 0 && width > 0) {
							domGeometry.setMarginBox(tab.mainContainer.domNode, {
								h: height,
								w: width
							});
							self._hideSearchCriteriaSplitter(tab);
							self._fixVerticalSearchTabLayoutSizes(tab);
							if (tab.mainContainer.resize) {
								tab.mainContainer.resize();
							}
						}
						return;
					}
					return original.apply(tab, arguments);
				};
			}));
		},

		_applyVerticalSearchCriteriaOverflowFix: function(tab) {
			if (!tab.searchCriteriaPane || !tab.searchCriteriaPane.domNode) {
				return;
			}
			domClass.add(tab.searchCriteriaPane.domNode, "metlifeSearchCriteriaPane");
			var paneH = domGeometry.getContentBox(tab.searchCriteriaPane.domNode).h;
			var headerH = tab.searchCriteriaHeader ? domGeometry.getMarginBox(tab.searchCriteriaHeader).h : 0;
			var bodyH = Math.max(0, paneH - headerH - 2);
			if (tab._searchCriteriaNode) {
				domStyle.set(tab._searchCriteriaNode, {
					height: bodyH + "px",
					minHeight: bodyH + "px",
					overflow: "visible",
					maxHeight: "none",
					boxSizing: "border-box"
				});
			}
			if (tab.searchDefinition && tab.searchDefinition.domNode) {
				domStyle.set(tab.searchDefinition.domNode, {
					height: bodyH + "px",
					minHeight: bodyH + "px",
					overflow: "visible",
					maxHeight: "none",
					boxSizing: "border-box"
				});
			}
			if (tab.searchDefinition && tab.searchDefinition.searchSection) {
				domStyle.set(tab.searchDefinition.searchSection, {
					overflow: "visible",
					height: "100%",
					minHeight: bodyH + "px",
					boxSizing: "border-box"
				});
			}
		},

		_fixVerticalSearchTabLayoutSizes: function(tab) {
			if (!tab || !tab.searchContainerNode || !tab.searchResultsContainer) {
				return;
			}
			if (!tab.searchContainerNode.className || tab.searchContainerNode.className.indexOf("metlifeVerticalSearchLayout") < 0) {
				return;
			}
			var containerBox = domGeometry.getContentBox(tab.searchContainerNode);
			if (!containerBox.h || !containerBox.w) {
				return;
			}
			this._relocateSearchCriteriaPaneToLeading(tab);
			this._hideSearchCriteriaSplitter(tab);
			this._applyVerticalSearchCriteriaOverflowFix(tab);
			var collapsed = this._isCriteriaCollapsed(tab);
			var criteriaWidth = this._getCriteriaPaneWidth(tab);
			domClass.toggle(tab.searchContainerNode, "metlifeCriteriaCollapsed", collapsed);
			if (tab.searchCriteriaPane && tab.searchCriteriaPane.domNode) {
				domClass.toggle(tab.searchCriteriaPane.domNode, "metlifeCriteriaCollapsed", collapsed);
			}
			domGeometry.setMarginBox(tab.mainContainer.domNode, {
				h: containerBox.h,
				w: containerBox.w
			});
			if (tab.searchCriteriaPane && tab.searchCriteriaPane.domNode) {
				domStyle.set(tab.searchCriteriaPane.domNode, {
					top: "0px",
					left: "0px",
					width: criteriaWidth + "px",
					minWidth: collapsed ? criteriaWidth + "px" : "280px",
					maxWidth: collapsed ? criteriaWidth + "px" : "45%",
					height: containerBox.h + "px",
					overflow: "hidden",
					boxSizing: "border-box"
				});
			}
			if (tab.searchResultsPane && tab.searchResultsPane.domNode) {
				domStyle.set(tab.searchResultsPane.domNode, {
					top: "0px",
					height: containerBox.h + "px",
					overflow: "hidden",
					boxSizing: "border-box"
				});
			}
			var resultsPaneBox = tab.searchResultsPane ? domGeometry.getContentBox(tab.searchResultsPane.domNode) : containerBox;
			var resultsHeaderH = tab.searchResultsHeader ? domGeometry.getMarginBox(tab.searchResultsHeader).h : 0;
			var resultsW = Math.max(200, resultsPaneBox.w - 2);
			var resultsH = Math.max(120, resultsPaneBox.h - resultsHeaderH - 2);
			domStyle.set(tab.searchResultsContainer.domNode, {
				width: resultsW + "px",
				height: resultsH + "px",
				overflow: "hidden",
				boxSizing: "border-box"
			});
			domGeometry.setMarginBox(tab.searchResultsContainer.domNode, {
				w: resultsW,
				h: resultsH
			});
			if (tab.searchResultsContainer.resize) {
				tab.searchResultsContainer.resize();
			}
			if (tab.searchResults && tab.searchResults.domNode) {
				domStyle.set(tab.searchResults.domNode, {
					width: "100%",
					height: "100%"
				});
			}
		},
		
		
		
		
		
.metlifeVerticalSearchLayout .ecmSearchMainContainer .searchArea,
.metlifeVerticalSearchLayout .ecmSearchMainContainer .metlifeSearchCriteriaPane {
	width: 340px !important;
	min-width: 280px !important;
	max-width: 45% !important;
	top: 0 !important;
	left: 0 !important;
	height: 100% !important;
	min-height: 100% !important;
	overflow: hidden !important;
	box-sizing: border-box;
	display: flex !important;
	flex-direction: column !important;
	border: 1px solid #ccc !important;
	background-color: #fff;
}

/* Collapsed Search Criteria: narrow strip so Search Results can maximize */
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .metlifeSearchCriteriaPane,
.metlifeVerticalSearchLayout .ecmSearchMainContainer .searchArea.metlifeCriteriaCollapsed,
.metlifeVerticalSearchLayout .ecmSearchMainContainer .metlifeSearchCriteriaPane.metlifeCriteriaCollapsed {
	width: 48px !important;
	min-width: 48px !important;
	max-width: 48px !important;
}

.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .dijitTitlePaneContentOuter,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .dijitTitlePaneContentInner,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .searchToggleArea,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .searchDefinition,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .metlifeCustomSearchPanel {
	display: none !important;
}

.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .searchTopArea,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .searchCriteriaHeader {
	flex: 1 1 auto;
	overflow: hidden;
}

.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .titleHeaderContainer,
.metlifeVerticalSearchLayout.metlifeCriteriaCollapsed .ecmSearchMainContainer .searchArea .titleHeader {
	width: 100% !important;
	max-width: 100% !important;
	overflow: hidden;
	box-sizing: border-box;
}

.metlifeVerticalSearchLayout .ecmSearchMainContainer .searchResultsPane {
	top: 0 !important;
	height: 100% !important;
	min-height: 100% !important;
	overflow: hidden !important;
	box-sizing: border-box;
}
