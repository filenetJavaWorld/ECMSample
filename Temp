/**
 * CNB Multi-Repository Search Plugin
 * 
 * This plugin provides a unified search interface across multiple P8 object stores.
 * It allows users to search documents across different repositories using a single
 * query, with results aggregated and displayed in a unified grid.
 * 
 * Features:
 * - Dynamic search fields configurable via JSON
 * - Multi-repository parallel search execution
 * - Aggregated results with repository identification
 * - Infinite scroll pagination
 * - Repository-specific field mapping
 * - Limits recent searches to max 3 items in the search template tree
 * - Removes Repository column from Favorites grid
 */

require([
	"dojo/_base/declare",
	"dojo/_base/lang",
	"dojo/_base/array",
	"dojo/aspect",
	"ecm/model/SearchTemplateTreeModel",
	"ecm/widget/listView/ContentList",
	"ecm/model/FavoritesResultSet"
], function(declare, lang, array, aspect, SearchTemplateTreeModel, ContentList, FavoritesResultSet) {
	
	// Plugin initialization
	console.log("CNB Multi-Repository Search Plugin loaded");

	// Remove Repository column from Favorites grid when result set is loaded into favoritesContent
	aspect.around(ContentList.prototype, "setResultSet", function(originalSetResultSet) {
		return function(resultSet, listParent) {
			if (resultSet && resultSet.isInstanceOf && resultSet.isInstanceOf(FavoritesResultSet)) {
				_removeRepositoryColumn(resultSet);
			}
			return originalSetResultSet.call(this, resultSet, listParent);
		};
	});

	function _removeRepositoryColumn(resultSet) {
		var REPOSITORY_FIELD = "repositoryName";
		// Remove from structure (Details view - columns.cells)
		if (resultSet.structure && resultSet.structure.cells) {
			for (var i = 0; i < resultSet.structure.cells.length; i++) {
				var cells = resultSet.structure.cells[i];
				if (array.isArray(cells)) {
					for (var j = cells.length - 1; j >= 0; j--) {
						if (cells[j] && cells[j].field === REPOSITORY_FIELD) {
							cells.splice(j, 1);
							break;
						}
					}
				}
			}
		}
		// Remove from magazineStructure (Magazine view)
		if (resultSet.magazineStructure && array.isArray(resultSet.magazineStructure)) {
			for (var k = resultSet.magazineStructure.length - 1; k >= 0; k--) {
				if (resultSet.magazineStructure[k] && resultSet.magazineStructure[k].field === REPOSITORY_FIELD) {
					resultSet.magazineStructure.splice(k, 1);
					break;
				}
			}
		}
	}

	// Override SearchTemplateTreeModel.getChildren to limit recent searches to max 3 childItems
	aspect.around(SearchTemplateTreeModel.prototype, "getChildren", function(originalGetChildren) {
		return function(parentItem, onComplete) {
			var self = this;
			var wrappedOnComplete = onComplete;
			if (parentItem === this.recentNode) {
				wrappedOnComplete = function(childItems) {
					if (childItems && childItems.length > 3) {
						childItems = childItems.slice(0, 3);
					}
					onComplete(childItems);
				};
			}
			return originalGetChildren.call(this, parentItem, wrappedOnComplete);
		};
	});
	
	/**
	 * Utility function to format dates consistently
	 */
	window.cnbFormatDate = function(dateValue) {
		if (!dateValue) return "";
		
		try {
			var date = new Date(dateValue);
			if (isNaN(date.getTime())) return dateValue;
			
			return date.toLocaleDateString() + " " + date.toLocaleTimeString();
		} catch (e) {
			return dateValue;
		}
	};
	
	/**
	 * Utility function to truncate long text
	 */
	window.cnbTruncateText = function(text, maxLength) {
		if (!text) return "";
		if (text.length <= maxLength) return text;
		return text.substring(0, maxLength - 3) + "...";
	};
});
