
		/**
		 * Resolves the search type for a saved template.
		 * New searches default to Both elsewhere; this only runs for persisted templates.
		 * Prefers moreOptions.metlifeSearchType (Both vs Documents cannot be inferred
		 * from OOTB objectType alone, since Both also saves as document).
		 */
		_getSavedSearchType: function(template) {
			if (!template) {
				return "both";
			}
			// moreOptions is restored by OOTB openSearchTemplate; application often is not.
			if (template.moreOptions && template.moreOptions.metlifeSearchType) {
				return this._normalizeAdvancedSearchType(template.moreOptions.metlifeSearchType);
			}
			var fromApplication = this._readApplicationAdvancedState(template);
			if (fromApplication && fromApplication.searchType) {
				return this._normalizeAdvancedSearchType(fromApplication.searchType);
			}
			if (template._metlifeAdvancedSearchType) {
				return this._normalizeAdvancedSearchType(template._metlifeAdvancedSearchType);
			}
			// Legacy saved Folders searches without MetLife metadata.
			if (template.objectType === "folder") {
				return "folders";
			}
			return "both";
		},
		
		
		
		
		2222222222222
		
		
		

		_persistAdvancedState: function(state, targetTemplate) {
			if (!state) {
				return;
			}
			var controls = state.controlsWidget;
			if (!controls) {
				return;
			}
			var def = state.def;
			var template = targetTemplate || (def && def.searchTemplate);
			if (!template) {
				return;
			}
			var searchType = this._normalizeAdvancedSearchType(controls.getSearchType());
			var sliceFolderId = controls.getSelectedSliceValue();
			var sliceLabel = state.selectedSlice && state.selectedSlice.label ?
				state.selectedSlice.label : null;
			this._writeAdvancedSearchTypeToTemplate(template, searchType, sliceFolderId, sliceLabel);
			if (def && def.searchTemplate && def.searchTemplate !== template) {
				this._writeAdvancedSearchTypeToTemplate(def.searchTemplate, searchType, sliceFolderId, sliceLabel);
			}
		},

		/**
		 * Writes Documents/Folders/Both onto the template ICN will save.
		 * moreOptions.metlifeSearchType is the restore key after refresh.
		 */
		_writeAdvancedSearchTypeToTemplate: function(template, searchType, sliceFolderId, sliceLabel) {
			if (!template) {
				return;
			}
			searchType = this._normalizeAdvancedSearchType(searchType);
			template._metlifeAdvancedSearchType = searchType;
			template._metlifeAdvancedSliceFolderId = sliceFolderId;
			if (sliceLabel) {
				template._metlifeAdvancedSliceLabel = sliceLabel;
			}
			if (!template.moreOptions) {
				template.moreOptions = {};
			}
			template.moreOptions.metlifeSearchType = searchType;
			var appPayload = {};
			if (template.application) {
				try {
					appPayload = typeof template.application === "string" ?
						JSON.parse(template.application) : template.application;
				} catch (e) {
					appPayload = {};
				}
			}
			if (!appPayload || typeof appPayload !== "object") {
				appPayload = {};
			}
			appPayload.metlifeAdvanced = {
				searchType: searchType,
				sliceFolderId: sliceFolderId,
				sliceLabel: sliceLabel
			};
			template.application = JSON.stringify(appPayload);
		},

		_readApplicationAdvancedState:
		
		
		
		333333
		
		_installAdvancedSearchPersistenceHooks: function(tab, state) {
			var def = tab.searchDefinition;
			if (!def || def._metlifeAdvancedPersistHook) {
				return;
			}
			def._metlifeAdvancedPersistHook = true;
			var self = this;
			// around (not after): first save builds a new template object; write onto that object.
			aspect.around(def, "_updateSearchTemplate", function(originalUpdate) {
				return function(searchTemplate) {
					var result = originalUpdate.apply(this, arguments);
					self._persistAdvancedState(state, searchTemplate);
					return result;
				};
			});
		},

		_installAdvancedSearchRestoreHooks: function(tab, state, tabKey) {
			var def = tab.searchDefinition;
			if (!def || def._metlifeAdvancedRestoreHook) {
				return;
			}
			def._metlifeAdvancedRestoreHook = true;
			def._metlifeAdvancedTabKey = tabKey;
			var self = this;
			if (def.onLoad) {
				aspect.after(def, "onLoad", function() {
					if (self._isNewSearchTemplate(def)) {
						return;
					}
					if (state.userControlsActive || state.controlsRestored) {
						return;
					}
					var matchedSlice = self._findSliceMatchingSearchInFolder(def.searchTemplate, def);
					if (!matchedSlice) {
						if (def._loaded) {
							state._savedTemplateRestoreAttempted = true;
						}
						return;
					}
					if (state._savedTemplateRestoreAttempted) {
						return;
					}
					state._savedTemplateRestoreAttempted = true;
					self._applyInitialAdvancedControls(tabKey, { initialRestore: true });
				});
			}
			aspect.after(def, "onSearchSaved", function(savedTemplate) {
				if (savedTemplate) {
					state.restoredTemplateId = self._getTemplateIdentity(savedTemplate);
					self._persistAdvancedState(state, savedTemplate);
				}
				state.controlsRestored = true;
				self._persistAdvancedState(state);
				return savedTemplate;
			});
		}
		
		
		
		
		
